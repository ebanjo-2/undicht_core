#ifndef KEY_IDS_H
#define KEY_IDS_H

namespace undicht {

    namespace window {

        namespace glfw {

            // updates the key ids in the window/key_ids.h file
            void setKeyIds();

        } // glfw

    } // window

} // undicht
#endif // KEY_IDS_H
